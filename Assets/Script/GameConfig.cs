using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

[CreateAssetMenu(menuName = "Game Config")]

public class GameConfig : ScriptableObject
{ 
    /*
    [Range(0f, 1f)] [SerializeField] float soundEffectsVolume = 0.5f;
    [Range(0f, 1f)] [SerializeField] float musicVolume = 0.5f;
    
    public Slider soundEffectsSlider;
    public Slider musicSlider;

    

    void Start()
    {
        soundEffectsSlider.value = soundEffectsVolume;
        musicSlider.value = musicVolume;
                
    }

    
    void Update()
    {
        musicVolume = musicSlider.value;
        soundEffectsVolume = soundEffectsSlider.value;
        //soundEffectsSlider.value = soundEffectsVolume;
        //musicSlider.value = musicVolume;
    }

    public void MusicMute(bool musicMute)
    {
        if (musicMute ==true)
        {
            musicSlider.value = 0;
        }
        else
        {
            musicSlider.value = 1f;
        }
    }


    public void SoundEffSlider(float sliderVolume)
    {
        soundEffectsVolume = sliderVolume;

    }
    public float SfxVolume()
    {
        return soundEffectsVolume;
    }

    public void MusicSlider(float sliderMusic)
    {
        musicVolume = sliderMusic;

    }
    public float MusicVolume()
    {
        return musicVolume;
    }
   */



}
